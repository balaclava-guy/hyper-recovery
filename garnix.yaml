builds:
  exclude:
    - "*.img"
    - "*.qcow2"
    - "*.iso"
  # By default garnix builds all packages in the flake.
  # We want it to build the compressed artifacts bundle.
  include:
    - packages.x86_64-linux.images-7z
