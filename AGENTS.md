1. Always prompt the user with a clear question before pushing changes to a remote repository, unless they have already instructed that you can push changes to a remote in a given turn.
2. For codebase context and architecture details, refer to the documentation in the `docs/` directory.
